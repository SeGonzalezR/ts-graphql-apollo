// import { appendUserJWT } from '../middelware'
// import admin from './admin'

// module.exports = app => {
//   if (process.env.NODE_ENV !== 'production') {
//     app.get('/', (req, res) => res.redirect('/graphql'))
//   }
//   app.use('/admin', admin)
//   app.use('/graphql', appendUserJWT)
// }
